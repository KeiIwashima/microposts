<%= form_for(current_user.iine_relationships.find_by(micropost_id: @micropost.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "いいね!済み", class: "btn" %>
<% end %>